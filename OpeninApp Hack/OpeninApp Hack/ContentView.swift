import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Color.gray.opacity(60) // Set the background color to a semi-transparent gray
            
            TabBarView()
        }
    }
}


